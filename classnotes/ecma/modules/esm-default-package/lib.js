function sum(a, b) {
  return a + b;
}

const minus = function (a, b) {
  return a - b;
};

const multiply = (a, b) => {
  return a * b;
};

const divide = (a, b) => a / b;

export default { sum, minus, multiply, divide };

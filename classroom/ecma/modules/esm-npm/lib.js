export function sum(a, b) {
  return a + b;
}

export const minus = function (a, b) {
  return a - b;
};

export const multiply = (a, b) => {
  return a * b;
};

export const divide = (a, b) => a / b;

export default { sum, minus, multiply, divide };
